{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ExtractionCandidates",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "label": {
        "type": "string",
        "minLength": 1,
        "maxLength": 160,
        "description": "Surface form exactly as observed in the source text"
      },
      "normalized": {
        "type": "string",
        "minLength": 1,
        "maxLength": 160,
        "description": "Canonical form used for deterministic comparisons"
      },
      "aliases": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160
        },
        "default": [],
        "maxItems": 10,
        "description": "Alternative spellings, acronyms, or boilerplate variants",
        "uniqueItems": true
      },
      "parents": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160
        },
        "description": "Textual parent anchors resolved by downstream logic",
        "default": []
      }
    },
    "required": ["label", "normalized", "parents"],
    "additionalProperties": false
  },
  "uniqueItems": true,
  "description": "Array sorted by lowercase normalized label for deterministic output"
}
